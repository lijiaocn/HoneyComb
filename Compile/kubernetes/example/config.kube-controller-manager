declare -A CONFIGS
# The IP address to serve on (set to 0.0.0.0 for all interfaces)
CONFIGS[address]='--address=0.0.0.0'
# Should CIDRs for Pods be allocated and set on the cloud provider.
CONFIGS[allocate-node-cidrs]='--allocate-node-cidrs=false'
# log to standard error as well as files
CONFIGS[alsologtostderr]='--alsologtostderr=false'
# The path to the cloud provider configuration file.  Empty string for no configuration file.
#CONFIGS[cloud-config]='--cloud-config=""'
# The provider for cloud services.  Empty string for no provider.
#CONFIGS[cloud-provider]='--cloud-provider=""'
# CIDR Range for Pods in cluster.
CONFIGS[cluster-cidr]='--cluster-cidr=172.16.1.0/17'
# The instance prefix for the cluster
CONFIGS[cluster-name]='--cluster-name="kubernetes"'
# The number of deployment objects that are allowed to sync concurrently. Larger number = more reponsive deployments, but more CPU (and network) load
CONFIGS[concurrent-deployment-syncs]='--concurrent-deployment-syncs=5'
# The number of endpoint syncing operations that will be done concurrently. Larger number = faster endpoint updating, but more CPU (and network) load
CONFIGS[concurrent-endpoint-syncs]='--concurrent-endpoint-syncs=5'
# The number of replication controllers that are allowed to sync concurrently. Larger number = more reponsive replica management, but more CPU (and network) load
CONFIGS[concurrent-rc-syncs]='--concurrent-rc-syncs=5'
# The number of resource quotas that are allowed to sync concurrently. Larger number = more responsive quota management, but more CPU (and network) load
CONFIGS[concurrent-resource-quota-syncs]='--concurrent-resource-quota-syncs=5'
# Number of nodes on which pods are bursty deleted in case of node failure. For more details look into RateLimiter.
CONFIGS[deleting-pods-burst]='--deleting-pods-burst=10'
# Number of nodes per second on which pods are deleted in case of node failure.
CONFIGS[deleting-pods-qps]='--deleting-pods-qps=0.1'
# Period for syncing the deployments.
CONFIGS[deployment-controller-sync-period]='--deployment-controller-sync-period=30s'
# Enable HostPath PV provisioning when running without a cloud provider. This allows testing and development of provisioning features.  HostPath provisioning is not supported in any way, won't work in a multi-node cluster, and should not be used for anything other than testing or development.
CONFIGS[enable-hostpath-provisioner]='--enable-hostpath-provisioner=false'
# The period for syncing the number of pods in horizontal pod autoscaler.
CONFIGS[horizontal-pod-autoscaler-sync-period]='--horizontal-pod-autoscaler-sync-period=30s'
# if non-empty, httptest.NewServer serves on this address and blocks
CONFIGS[httptest.serve]='--httptest.serve=""'
# Burst to use while talking with kubernetes apiserver
CONFIGS[kube-api-burst]='--kube-api-burst=30'
# QPS to use while talking with kubernetes apiserver
CONFIGS[kube-api-qps]='--kube-api-qps=20'
# Path to kubeconfig file with authorization and master location information.
#CONFIGS[kubeconfig]='--kubeconfig=../../example/kubeconf.kube-controller-manager.yml'
CONFIGS[kubeconfig]='--kubeconfig=./kubeconf.kube-controller-manager.yml'
# Start a leader election client and gain leadership before executing the main loop. Enable this when running replicated components for high availability.
CONFIGS[leader-elect]='--leader-elect=false'
# The duration that non-leader candidates will wait after observing a leadership renewal until attempting to acquire leadership of a led but unrenewed leader slot. This is effectively the maximum duration that a leader can be stopped before it is replaced by another candidate. This is only applicable if leader election is enabled.
CONFIGS[leader-elect-lease-duration]='--leader-elect-lease-duration=15s'
# The interval between attempts by the acting master to renew a leadership slot before it stops leading. This must be less than or equal to the lease duration. This is only applicable if leader election is enabled.
CONFIGS[leader-elect-renew-deadline]='--leader-elect-renew-deadline=10s'
# The duration the clients should wait between attempting acquisition and renewal of a leadership. This is only applicable if leader election is enabled.
CONFIGS[leader-elect-retry-period]='--leader-elect-retry-period=2s'
#0: when logging hits line file:N, emit a stack trace
#CONFIGS[log-backtrace-at]='--log-backtrace-at='
# If non-empty, write log files in this directory
CONFIGS[log-dir]='--log-dir=""'
# Maximum number of seconds between log flushes
CONFIGS[log-flush-frequency]='--log-flush-frequency=5s'
# log to standard error instead of files
CONFIGS[logtostderr]='--logtostderr=true'
# The address of the Kubernetes API server (overrides any value in kubeconfig)
# CONFIGS[master]='--master=""'
# The resync period in reflectors will be random between MinResyncPeriod and 2*MinResyncPeriod
CONFIGS[min-resync-period]='--min-resync-period=12h0m0s'
# The period for syncing namespace life-cycle updates
CONFIGS[namespace-sync-period]='--namespace-sync-period=5m0s'
# Amount of time which we allow running Node to be unresponsive before marking it unhealty. Must be N times more than kubelet's nodeStatusUpdateFrequency, where N means number of retries allowed for kubelet to post node status.
CONFIGS[node-monitor-grace-period]='--node-monitor-grace-period=40s'
# The period for syncing NodeStatus in NodeController.
CONFIGS[node-monitor-period]='--node-monitor-period=5s'
# Amount of time which we allow starting Node to be unresponsive before marking it unhealty.
CONFIGS[node-startup-grace-period]='--node-startup-grace-period=1m0s'
# The period for syncing nodes from cloudprovider. Longer periods will result in fewer calls to cloud provider, but may delay addition of new nodes to cluster.
CONFIGS[node-sync-period]='--node-sync-period=10s'
# The grace period for deleting pods on failed nodes.
CONFIGS[pod-eviction-timeout]='--pod-eviction-timeout=5m0s'
# The port that the controller-manager's http service runs on
CONFIGS[port]='--port=10252'
# Enable profiling via web interface host:port/debug/pprof/
CONFIGS[profiling]='--profiling=true'
# the increment of time added per Gi to ActiveDeadlineSeconds for an NFS scrubber pod
CONFIGS[pv-recycler-increment-timeout-nfs]='--pv-recycler-increment-timeout-nfs=30'
# The minimum ActiveDeadlineSeconds to use for a HostPath Recycler pod.  This is for development and testing only and will not work in a multi-node cluster.
CONFIGS[pv-recycler-minimum-timeout-hostpath]='--pv-recycler-minimum-timeout-hostpath=60'
# The minimum ActiveDeadlineSeconds to use for an NFS Recycler pod
CONFIGS[pv-recycler-minimum-timeout-nfs]='--pv-recycler-minimum-timeout-nfs=300'
# The file path to a pod definition used as a template for HostPath persistent volume recycling. This is for development and testing only and will not work in a multi-node cluster.
#CONFIGS[pv-recycler-pod-template-filepath-hostpath]='--pv-recycler-pod-template-filepath-hostpath=""'
# The file path to a pod definition used as a template for NFS persistent volume recycling
#CONFIGS[pv-recycler-pod-template-filepath-nfs]='--pv-recycler-pod-template-filepath-nfs=""'
# the increment of time added per Gi to ActiveDeadlineSeconds for a HostPath scrubber pod.  This is for development and testing only and will not work in a multi-node cluster.
CONFIGS[pv-recycler-timeout-increment-hostpath]='--pv-recycler-timeout-increment-hostpath=30'
# The period for syncing persistent volumes and persistent volume claims
CONFIGS[pvclaimbinder-sync-period]='--pvclaimbinder-sync-period=10m0s'
# The period for syncing quota usage status in the system
CONFIGS[resource-quota-sync-period]='--resource-quota-sync-period=5m0s'
# If set, this root certificate authority will be included in service account's token secret. This must be a valid PEM-encoded CA bundle.
#CONFIGS[root-ca-file]='--root-ca-file=""'
# Filename containing a PEM-encoded private RSA key used to sign service account tokens.
CONFIGS[service-account-private-key-file]='--service-account-private-key-file=../../../../AuthnAuthz/serviceAccount/ca/ca-key.pem'
# The period for syncing services with their external load balancers
CONFIGS[service-sync-period]='--service-sync-period=5m0s'
# logs at or above this threshold go to stderr
CONFIGS[stderrthreshold]='--stderrthreshold=2'
# Number of terminated pods that can exist before the terminated pod garbage collector starts deleting terminated pods. If <= 0, the terminated pod garbage collector is disabled.
CONFIGS[terminated-pod-gc-threshold]='--terminated-pod-gc-threshold=12500'
# log level for V logs
CONFIGS[v]='--v=0'
# Print version information and quit
CONFIGS[version]='--version=false'
# comma-separated list of pattern=N settings for file-filtered logging
CONFIGS[vmodule]='--vmodule='
