等待走读的程序:

	kube-apiserver
	kube-controller-manager
	kube-scheduler
	kube-proxy
	kubelet

等待研究的课题:

	sdn网络
	容器监控(cadvisor)
	用于服务发现的键值存储系统(etcd)
	Linux与容器技术(docker、rocket等)

需要关注的问题:

	访问控制/安全策略
	高效运维(集群状态监测、进程监测、日志汇总等)


kube-apiserver:
1. 命令行增加日志开关
2. 关闭insecure接口
3. 将日志写入到专门的日志系统
